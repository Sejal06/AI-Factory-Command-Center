# AI-Factory-Command-Center
OEE = Availability × Performance × Quality
Production = bottleneck × OEE
Demand = base × exp(-price effect)
Sales = min(production, demand)
Revenue = sales × price
Objective:
OEE is tracked separately
Production planning is separate
Pricing / demand is separate
Supply chain is separate
